Start EM Session 7 — ship §11 EM & Relativity (5 topics, FIG.58–62). Smaller session by count than Session 6 (which shipped 6) and dramatically smaller than Session 5 (14), but the **emotional apex of the entire branch**. §11.4 `magnetism-as-relativistic-electrostatics` is THE reveal the branch was built toward: two parallel current-carrying wires in the lab frame (magnetic attraction) boosted into the electron rest frame (length contraction of the lattice produces a net + charge density → Coulomb attraction). Same force, different name. The single most surprising result in classical physics. The Sessions 4/5/6 parallel-agent dispatch pattern still applies; dispatch all 5 in parallel once the scaffold + (optional) primitive land.

Spec: docs/superpowers/specs/2026-04-22-electromagnetism-branch-design.md
— read §11 section + "Calculus policy" + "Voice" + "Integration checklist" + "Risk notes" (the §11 SR-prerequisite is called out explicitly — "this module assumes *minimal* special relativity — length contraction, time dilation, Lorentz transform of coordinates. Topic 61 introduces just enough inline; full treatment belongs in the future RELATIVITY branch") before doing anything.
— Spec progress is now 57/66 (86%); update to 62/66 (94%) at session end. **After this session, only §12 Foundations remains — 4 topics across 1 module. One session to branch completion.**

Sessions 1–6 precedents:
— Plans:
    docs/superpowers/plans/2026-04-22-electromagnetism-session-1-electrostatics.md
    docs/superpowers/plans/2026-04-22-electromagnetism-session-2-fields-in-matter-magnetostatics.md
    docs/superpowers/plans/2026-04-22-electromagnetism-session-3-magnetism-in-matter-induction.md
    docs/superpowers/plans/2026-04-23-electromagnetism-session-4-circuits-maxwell.md
    docs/superpowers/plans/2026-04-23-electromagnetism-session-5-em-waves-optics.md
    docs/superpowers/plans/2026-04-24-electromagnetism-session-6-radiation.md
— Implementation logs in MemPalace, wing=physics, room=decisions:
    implementation_log_em_session_1_electrostatics.md
    implementation_log_em_session_2_fields_in_matter_magnetostatics.md
    implementation_log_em_session_3_magnetism_in_matter_induction.md
    implementation_log_em_session_4_circuits_maxwell.md
    implementation_log_em_session_5_em_waves_optics.md
    implementation_log_em_session_6_radiation.md — **READ THIS FIRST.** Confirms (a) the 6-parallel-agent dispatch pattern held cleanly with no contract violations (down from 14 in Session 5 — the smaller fan-out had less coordination friction), (b) NO shared primitive was built (DipoleRadiationCanvas was deferred — the spec's amortization threshold for a 6-topic module wasn't crossed; each topic handled field-line / polar-pattern drawing inline in Canvas 2D), and the call held — no duplication pain reported by Wave 2 agents, (c) Larmor + Hertz + Dirac landed in PHYSICISTS; Marconi + Abraham were judged optional and skipped (Hertz 1888 carries §10.3's narrative without Marconi; Abraham-Lorentz lineage gets a one-line aside in §10.6 prose without a Physicist entry — credit Lorentz instead since he's already in PHYSICISTS), (d) `ALPHA_FINE = 7.2973525693e-3` landed in `lib/physics/constants.ts` cleanly — §10.1 Larmor used it for the classical-electron-radius / Thomson-cross-section discussion, (e) the §10.6 radiation-reaction "honesty moment" landed at the target tone — Abraham-Lorentz 1903, runaway solutions, pre-acceleration paradox, Landau-Lifshitz reduced-order, Dirac 1938 ALD, the QED renormalisation epilogue. The seam paragraph passed the "Kurzgesagt voice" check, (f) **NEW finding from Session 6 Wave 2.5:** the `<Term>` aside-link convention also works for proper nouns that aren't physicists per se (engineering/instrument names like `hertzian-dipole`, `abraham-lorentz-equation`) — these stay as glossary entries, not physicist entries, even when their etymology is a person. Use `<Term slug="abraham-lorentz-equation" />` not `<PhysicistLink slug="max-abraham" />`, (g) **CONFIRMED gotcha from Session 5:** `pnpm content:publish` still hashes raw MDX source, not parsed block output. The 15-minute CLI patch flagged in Session 5 was NOT shipped this session (scope-saving). Session 7 should consider it **before any parser change lands** — see gotcha §C below. If a parser-only fix is needed mid-Session-7, use `--force` as a workaround, (h) **NO §11/§12 forward-refs surfaced from §10 MDX** — Wave 3's MDX scan came back clean. Session 7's own forward-refs (likely `aharonov-bohm-effect`, `gauge-theory-origins` → §12) will be the first §12-pointing placeholders in the branch.

Workflow:
1. First, write the Session 7 plan via /superpowers:writing-plans. Mirror the Session 6 plan's 4-wave structure but adapted to 5 topics. Probable shape:
   - **Wave 0** — NONE. Sessions 5–6 closed all backfill loops; no carried-over Supabase inconsistencies expected. (If a new parser issue surfaces during §11 MDX authoring — e.g., the field-tensor topic's heavy use of `F^{\mu\nu}` superscripts/subscripts triggers a micromark edge case — reinstate Wave 0 as a mini-backfill afterwards. Tensor notation is the densest LaTeX in the branch so far; treat it as a possible parser stress test.)
   - **Wave 1** (serial, 1 agent): scaffold `branches.ts` (5 new topic entries FIG.58–62), `physicists.ts` (1–3 new — **Hermann Minkowski (definitely)** for §11.3 field-tensor; **Wilhelm Weber (optional)** for §11.1 charge-invariance / Weber-Kohlrausch 1856 c-measurement; possibly nothing else since Lorentz + Einstein + Maxwell + Faraday are already present from Sessions 1–3), `glossary.ts` (~12–15 new terms across charge invariance, Lorentz transform of fields, field tensor, four-potential, EM Lagrangian). **Possible placeholder promotions:** scan Sessions 4–5 seed scripts for any forward-refs pointing at §11 — likely candidates `field-tensor`, `four-potential`, `lorentz-boost-em`, `electromagnetic-field` (may already be promoted via §07 + §08). Session 6's implementation log confirmed NO §11 forward-refs surfaced, but check Sessions 2 (`fresnel-equations` precedent) + 4 (3 forward-refs) + 5 (2 forward-refs + 2 promotions) seed scripts directly to be sure.
   - **Wave 1.5** (OPTIONAL, decide at plan time): a `LorentzBoostCanvas` primitive. §11 has TWO topics that draw the same picture from different angles — §11.2 `e-and-b-under-lorentz` (drawing E and B vectors transforming under a boost) and §11.4 `magnetism-as-relativistic-electrostatics` (drawing the two-wire setup in the lab frame and then boosted to the electron rest frame). A shared primitive that handles "boost a 2D snapshot of fields/charges by velocity v" might amortize. **Recommendation: defer to scene-level implementation UNLESS the author agent reports duplication pain** — 5 topics is below the threshold the CircuitCanvas (7) + RayTraceCanvas (10) crossed, and Session 6 (6 topics) got away cleanly without one. The two §11 topics that share the boost-visualization burden may benefit from extracting a small shared helper module (`lib/physics/electromagnetism/lorentz-boost.ts`) without going as far as a full Canvas primitive. Decide after reading the spec's §11 section + money-shot paragraph. **CAVEAT: §11.4 is the branch's emotional apex** — if a primitive helps the money shot land cleaner, build it. If it adds noise without adding clarity, skip it.
   - **Wave 2** (5 parallel agents): per-topic authoring. Standard Canvas 2D with the EM palette (magenta E / cyan B / amber radiation / green-cyan induced-current / lilac field-energy density / pale-blue wavefronts / orange-red bremsstrahlung-X-ray from Session 6). §11 topics are field-level + relativistic-frame-level — drawing **two synchronized panels** is the dominant visual idiom. §11.4 specifically: lab-frame on the left (uncharged wires + B-field), boosted-frame on the right (length-contracted lattice → net charge density → E-field replacing B-field). Same vertical-slice pattern as Session 6: physics lib + vitest + 3 scenes + page.tsx + content.en.mdx per topic. Parallel-safety contract unchanged — flag the Session 5 MDX gotchas (no nested JSX in `title=""`, single-line math only, no bare `<` in prose) plus the **NEW §11-specific gotcha**: `<EquationBlock tex={...}>` for tensor notation needs **double backslashes throughout** (`F^{\\mu\\nu}`, `\\partial_\\mu A^\\mu`, `\\Lambda^\\mu{}_\\nu`) — the field-tensor topic agent should write a few representative tensor equations early and run a local `pnpm content:publish` check before authoring all 5 sections, to catch escape issues before they pile up.
   - **Wave 2.5** (serial orchestrator): 5 registry merges + 5 publishes + 5 per-topic commits. Order: FIG.58 → FIG.62. If a publish shows `0 inserted / 0 updated`, the MDX parsed empty — investigate before committing. **§11.3 field-tensor is the highest-risk publish of the session** because it has the densest LaTeX; if it fails, fix and re-run before moving to §11.4.
   - **Wave 3** (1 agent): seed-em-07.ts for new physicists + glossary prose; curl sweep of 5 URLs; spec progress → 62/66 (94%); update spec header status line; MemPalace log.

2. Then execute via /superpowers:subagent-driven-development, token-saving variant — no per-task review, parallel waves, per wing=physics feedback_execution_style.

CRITICAL gotchas carried from Sessions 1–6 (MUST be in the Session 7 plan):

(A) Seed script for new physicists + glossary prose. `PhysicistLink` and `Term` components fetch from Supabase `content_entries` at render time; TS arrays are structural metadata only. → Wave 3 creates `scripts/content/seed-em-07.ts` mirroring `seed-em-06.ts` exactly (same `PhysicistSeed` / `GlossarySeed` interfaces, same `paragraphsToBlocks` / `parseMajorWork` / `main()` loop). Per-entry discipline from Sessions 5–6: bios ~300–400 words (3 paragraphs), glossary descriptions ~150–250 words (1–2 paragraphs). Tight and factual — no padding.

(B) Forward-reference placeholders. Session 6 surfaced none. Session 7 likely surfaces 1–3 pointing at §12: `gauge-theory-origins` (referenced from §11.5 four-potential's gauge-freedom recap), `aharonov-bohm-effect` (referenced from §11.3 field-tensor or §11.5 four-potential as the "potential matters even where the field is zero" forward-ref), possibly `magnetic-monopole` (referenced from §11.3 if the dual tensor *F^{μν} discussion mentions "if monopoles existed, the dual would source them"). Scan all 5 MDX files at Wave 3 start; add placeholders for any unresolved slug with the `// FORWARD-REF: full treatment in §12.X` pattern from `seed-em-04.ts:637–665`.

(C) `pnpm content:publish --force` for parser-affected re-publishes. The CLI hashes raw MDX source, not parsed output — so a parser fix won't trigger re-publish without `--force`. **Strong recommendation: ship the 15-minute CLI patch BEFORE Wave 1** changing the hash source from raw string to `JSON.stringify({title, subtitle, blocks, aside_blocks, meta})` so parser-only fixes self-refresh. §11.3 field-tensor's heavy LaTeX is exactly the kind of content where a mid-session parser tweak might be needed; pre-patching the CLI removes a potential mid-session blocker. If skipping the patch, document `--force` usage in the plan's "known gotchas" section as a workaround.

(D) MDX authoring gotchas (Session 5 Wave 2.5 discoveries, all still active): (i) `<PhysicistLink slug="..."` inside a `title="..."` JSX attribute breaks quoting because of the nested `"` — keep human links in body prose, not in `title=` attributes. (ii) Multi-line `$$ ... \qquad<newline> ... $$` math blocks choke micromark — collapse to single-line `$$...$$`. (iii) Bare `<` in prose can parse as a JSX tag opener (Session 5 example: `"~4% to <0.5%"`) — reword to `"down to well under 0.5%"` or escape as `&lt;`. **NEW Session 7 risk: tensor-index syntax.** `F^{\mu\nu}` must be `F^{\\mu\\nu}` inside a `tex={\`...\`}` template literal. `\partial_\mu A^\mu` must be `\\partial_\\mu A^\\mu`. The field-tensor topic agent should write a known-good test equation first (`F_{\\mu\\nu} = \\partial_\\mu A_\\nu - \\partial_\\nu A_\\mu`), publish, verify the rendered KaTeX, then continue. Bake all four rules into the Wave 2 agent prompt.

(E) Shared types between primitive and Wave 2 agents. `Vec3` from `@/lib/physics/electromagnetism/lorentz` for 3D vectors, `Vec2` from `@/lib/physics/coulomb` for 2D. **NEW §11-specific consideration:** §11 is the first module in the branch where four-vectors and tensors appear. Decide at plan time whether to add a `Tensor4` / `FourVector` type to `@/lib/physics/electromagnetism/relativity.ts` (new file) — likely yes, since §11.3 field-tensor needs `FieldTensor = number[4][4]` and §11.5 four-potential needs `FourPotential = [φ/c, A_x, A_y, A_z]`. Define these in Wave 1's scaffold so all 5 Wave 2 agents share canonical types. If you build a `LorentzBoostCanvas` primitive in Wave 1.5, enforce its types as canonical (no local fallbacks) via the parallel-safety contract.

(F) §11 physics-lib nesting convention. Continue the Session 2-established `lib/physics/electromagnetism/<topic>.ts` layout. §11 introduces `charge-invariance.ts`, `lorentz-boost-em.ts` (or `lorentz-em-fields.ts`), `field-tensor.ts`, `magnetism-relativistic.ts`, `four-potential.ts`. Reuse constants from `@/lib/physics/constants` (`MU_0`, `EPSILON_0`, `SPEED_OF_LIGHT`, `BOHR_MAGNETON`, `ALPHA_FINE`). **Possible new constant:** none expected — the speed of light covers γ, β, η. If the four-potential Lagrangian topic wants Planck units for the QED-classical-limit forward-ref to §12.4, those would belong in §12's plan, not §11.

(G) §11 voice — MAGNETISM IS ELECTRICITY SEEN FROM A MOVING TRAIN. The ENTIRE branch was building toward this. Every E-field-vs-B-field distinction we drew through §03 → §07 → §08 was a Faustian bargain — pedagogically necessary but ontologically wrong. There is no second force; magnetism is the relativistic correction to electrostatics. §11.1 charge-invariance establishes the one quantity every observer must agree on (without invariance, the whole edifice falls). §11.2 e-and-b-under-lorentz shows the transformation rules — what looks like a magnet to you looks like an electric field to someone moving past. §11.3 field-tensor F^{μν} packages the six numbers (3 E components + 3 B components) into one 4×4 antisymmetric object; the dual tensor *F^{μν} swaps E↔B and is what monopoles would source if they existed (forward-ref to §12.3). §11.4 IS THE MONEY SHOT — two parallel wires, lab frame: magnetic attraction. Boost to electron rest frame: lattice length-contracts, net + charge density appears, Coulomb attraction. Same force. §11.5 four-potential A^μ = (φ/c, **A**) and the EM Lagrangian L = −¼F_{μν}F^{μν} − A_μJ^μ rewrites the whole branch as a principle of least action — the cleanest sentence in the entire physics corpus. **The §11 voice payoff:** write §11.4 as "the moment a century of electromagnetism and a decade of special relativity snapped together." This is the line readers will quote. Don't miss it.

New physicists needed (check existing 100 PHYSICISTS first after Session 6 added Larmor, Hertz, Dirac; only add missing):
— §11.3 the-electromagnetic-field-tensor: **Hermann Minkowski (1864–1909).** German mathematician. Einstein's former teacher at ETH Zürich (Einstein later remarked Minkowski was "lazy" as a student — the relationship was mutual irritation). 1907–1908: realized special relativity's symmetry was a 4D pseudo-Euclidean geometry where the spacetime interval ds² = c²dt² − dx² − dy² − dz² is invariant — and that this geometry made the field tensor F^{μν} a natural object. His 1908 Cologne lecture: "Henceforth space by itself, and time by itself, are doomed to fade away into mere shadows, and only a kind of union of the two will preserve an independent reality." Died at 44 of acute appendicitis in 1909, four years after publishing the 4D formulation that gave Einstein's 1905 SR a mathematical home. Also: independently developed the geometry of numbers; mentor to David Hilbert at Königsberg.
— §11.1 charge-invariance (optional, skip if narrative flows without): **Wilhelm Eduard Weber (1804–1891).** German physicist. 1856 with Rudolf Kohlrausch measured the ratio of electrostatic-to-electromagnetic units of charge and found it equal to the speed of light — six years before Maxwell's 1862 prediction that light IS an electromagnetic wave. The Weber-Kohlrausch experiment is the first quantitative hint that c was hiding inside electromagnetism. Also: Weber's law of electrodynamics (now obsolete, replaced by Maxwell), Göttingen Seven dismissal in 1837, partnered with Gauss on terrestrial magnetism. Decide at author time — the Weber-Kohlrausch story is the canonical hook for "c was lurking in EM constants long before relativity," but §11.1 can land cleanly with just Maxwell-Faraday-Lorentz from existing PHYSICISTS. Include if §11.1 prose wants the historical depth.
Append related topics:
— `albert-einstein` (existing): add §11.2 e-and-b-under-lorentz, §11.3 the-electromagnetic-field-tensor, §11.4 magnetism-as-relativistic-electrostatics, §11.5 four-potential-and-em-lagrangian (the entire module is downstream of his 1905 SR paper).
— `hendrik-antoon-lorentz` (existing): add §11.2 e-and-b-under-lorentz, §11.4 magnetism-as-relativistic-electrostatics (the boost transformation bears his name; the magnetism-as-relativistic reveal IS the Lorentz-force law in disguise).
— `james-clerk-maxwell` (existing): add §11.5 four-potential-and-em-lagrangian (his 1865 vector-potential A is what becomes the four-potential's spatial part).
— `edward-mills-purcell` (existing, added in earlier session): add §11.4 magnetism-as-relativistic-electrostatics — Purcell's *Electricity and Magnetism* (Berkeley Physics Course, 1965) is the canonical pedagogical source for the two-wire boost derivation. Almost every undergrad textbook teaches §11.4 via Purcell's diagrams. Surface him in the §11.4 prose as the modern teacher of the result.
— `joseph-louis-lagrange` (existing, from CM): add §11.5 four-potential-and-em-lagrangian — his action principle is the structure §11.5 cashes out for fields.
— `emmy-noether` (existing, from CM): add §11.5 four-potential-and-em-lagrangian — gauge symmetry → conservation of charge is the canonical Noether's-theorem application that §11.5 should at least gesture at (full treatment is §12.1 gauge-theory-origins, but the §11.5 → §12.1 bridge is one of the cleanest narrative arcs in the branch).
That's 1–2 new entries (Minkowski definite, Weber optional). No changes to existing physicists' *slug* or *born*/*died* fields; only `relatedTopics` appends.

New glossary terms likely needed (check existing GLOSSARY after Session 6):
— §11.1 charge-invariance (~2 terms): `charge-invariance`, `electromagnetic-units-ratio` (the Weber-Kohlrausch quantity, optional — skip if Weber not added).
— §11.2 e-and-b-under-lorentz (~3 terms): `lorentz-transformation-of-fields` (or `field-lorentz-transform`), `boost-mixing-e-and-b`, `e-dot-b-invariant` (the Lorentz invariant E·B; pairs with E²−c²B² which may need a separate term `lorentz-invariants-of-em-field`).
— §11.3 field-tensor (~3 terms): `electromagnetic-field-tensor` (or `faraday-tensor`), `dual-field-tensor` (or `hodge-dual-tensor`), `four-current` (J^μ = (cρ, **J**)). `four-vector` may belong here too as a foundational entry, or defer to §12 if it's used more there.
— §11.4 magnetism-as-relativistic-electrostatics (~2 terms): `length-contraction-of-current` (the specific application — distinct from generic SR length contraction), `relativistic-electrostatic-equivalence` (or fold into the topic itself; trim if redundant).
— §11.5 four-potential (~3 terms): `four-potential`, `lorenz-gauge` (note: Lorenz with no T — Ludvig Lorenz, Danish, 1867, NOT Hendrik Lorentz), `em-lagrangian-density`. The `gauge-transformation` term already exists from §03 + §07; cross-link rather than duplicate.
Total realistic: ~12–14 new terms. Trim at author time if narrative flows tighter.

**SLUG-COLLISION WATCH:** `lorenz-gauge` vs `lorentz-transformation-of-fields` — these are TWO DIFFERENT PEOPLE (Ludvig Lorenz, gauge condition 1867 / Hendrik Lorentz, transformation 1895). The most-misspelled pair of names in physics. Triple-check spellings in all MDX, glossary, and physicist references; the publish CLI will not catch this kind of error.

Module dependencies (per spec):
— §11 EM & Relativity: REQUIRES §07 Maxwell's equations (the four equations are what get rewritten in tensor form). ☑ Shipped Session 4.
— §11 ALSO requires minimal SR — length contraction, time dilation, Lorentz transform of coordinates. **INLINE in §11.4 specifically** the way the spec dictates: just enough to land the money shot, then a margin aside pointing at the future RELATIVITY branch for the full treatment.
— §11.5 four-potential cross-refs §07.3 gauge-freedom-and-potentials (existing) — the Lorenz gauge ∂_μA^μ = 0 is the relativistic completion of §07.3's Coulomb / Lorenz gauge treatment.
— §11.3 field-tensor forward-refs §12.3 magnetic-monopoles-and-duality — the dual tensor *F^{μν} swaps E↔B and is what monopoles would source. Add `magnetic-monopole` placeholder if §11.3 prose includes the dual.
— §11.5 forward-refs §12.1 gauge-theory-origins — Noether's theorem gauge → charge conservation is the §11.5-to-§12.1 bridge.

Money shot to nail:
— **§11.4 magnetism-as-relativistic-electrostatics.** Two-panel synchronized animation. Left panel (lab frame): two parallel wires carrying current I_1 and I_2 in the same direction. Static lattice of + ions (drawn as a row of magenta dots, equally spaced). Drift electrons moving with velocity v (drawn as cyan dots flowing). Wires are net-neutral. Magnetic field B between them (cyan field arrows). Force F_mag = (μ₀ I₁ I₂ / 2π d) × L, attractive. Right panel (boosted to electron rest frame at v_drift): the cyan electrons are now at rest. The magenta lattice is now moving in the OPPOSITE direction at v_drift. Length contraction: lattice spacing decreases by factor γ = 1/√(1−v²/c²). Net + charge density appears (the cyan dot count per unit length stays the same, but the magenta dot count per unit length INCREASED). Coulomb attraction E-field (magenta arrows now). Force F_elec equals F_mag exactly. **Same force, different name.** Slider: drift velocity v (in scaled units, e.g. 0 to 0.99c with logarithmic visual mapping since real drift velocities are mm/s but the relativistic effect at any v works the same — be honest about this in the prose). Both panels annotated with the force calculation. The "click" moment when the reader sees the lattice spacing contract on the right and the + charge density appear is THE moment the branch closes.

Honest moment to nail:
— **§11.5 four-potential-and-em-lagrangian.** This is the cleanest sentence in physics. L = −¼F_{μν}F^{μν} − A_μJ^μ. Six terms in the field tensor square, one source-coupling term. Apply Euler-Lagrange to A^μ — out comes Maxwell's equations. Apply Noether's theorem to gauge symmetry — out comes charge conservation. The entire edifice of classical electromagnetism is one line. Write it as the climactic moment of the entire branch — every §01 through §10 has been an unfolding of this single Lagrangian. The spec voice section: "Electromagnetism rewritten as a principle of least action." 1300–1500 words. Don't rush the derivation; let the Euler-Lagrange step land. Include the Noether → charge conservation gesture even though full treatment is §12.1 — the bridge sentence is what makes §11.5 → §12.1 feel inevitable rather than tacked-on.

Voice reminder:
— §11 is the REVEAL. Magnetism is electricity seen from a moving train. Charge invariance + length contraction + the 1865 Maxwell vector potential + the 1908 Minkowski 4D geometry = one tensor F^{μν} containing both E and B, transforming linearly under Lorentz boosts, sourced by the four-current J^μ, derived from one Lagrangian, conserving one quantity (charge) via Noether. **Write it as the historical inevitability it is.** §11.1 sets charge invariance as the one fixed point. §11.2 derives the field transformations. §11.3 packages them. §11.4 cashes them out as the most surprising result in classical physics. §11.5 closes the loop — the whole branch is one line of action. The module ends the "what is electromagnetism?" arc and sets up §12 (gauge as the symmetry behind the symmetry, monopoles as the missing dual, AB effect as the potential's revenge, QED as the classical limit's other side).

When done, end-of-session deliverables:
— 5 new topics live at /en/electromagnetism/<slug> (200 OK on all 5)
— Optional LorentzBoostCanvas primitive (if Wave 1.5 judges it worthwhile) with green vitest suite
— Hermann Minkowski added to PHYSICISTS (and optionally Wilhelm Weber)
— ~12–14 new glossary entries seeded
— `Tensor4` / `FourVector` / `FieldTensor` types in `lib/physics/electromagnetism/relativity.ts` (new file)
— 5 per-topic feat commits + Wave 1 scaffold + optional Wave 1.5 primitive + Wave 3 seed + Wave 3 spec-update commits (expected total: 9 commits; 10 if primitive lands; 11 if `content:publish` CLI hash-source patch ships as a quality-of-life improvement)
— Spec progress table updated to 62/66 (94%) — **only 4 topics remain in §12 Foundations**
— Implementation log filed to MemPalace as implementation_log_em_session_7_em_relativity.md, with particular attention to: (a) did you build a LorentzBoostCanvas primitive or handle two-panel boost-visualization topic-by-topic, and if topic-by-topic did duplication pain emerge between §11.2 and §11.4? (b) how did the §11.4 money shot land — did the lattice-contraction → net-charge-density visual click on first viewing, or did the prose have to do the heavy lifting? (c) how did the §11.5 four-potential closing land — did "the whole branch is one line of action" feel earned or tacked-on? (d) did the field-tensor LaTeX (F^{\\mu\\nu}, \\partial_\\mu, \\Lambda^\\mu{}_\\nu) round-trip cleanly through the publish CLI, or did it surface a new MDX gotcha? (e) any new forward-refs pointing at §12? (f) was Minkowski's bio voice ("Einstein's lazy student who gave SR its geometry") right, or did it overshoot? (g) did the `lorenz-gauge` vs `lorentz-transformation` slug-collision watch hold, or did the spelling slip somewhere?

Next session: §12 Foundations (4 topics, FIG.63–66). Gauge invariance and the birth of gauge theories (Weyl 1929 → Yang-Mills 1954 → Standard Model template), Aharonov-Bohm effect (1959, the potential matters even where the field is zero — two-slit electron interference with shielded solenoid), magnetic monopoles and duality (Dirac 1931, the symmetric Maxwell, the particle we haven't found), classical limit of QED (where classical EM ends and what replaces it — coherent states, photon-counting in the high-photon-number limit, anomalous magnetic moment as the quantum correction). The module's money shot is **§12.2 aharonov-bohm-effect** — two-slit electron interference fringes shifting as the shielded solenoid's enclosed flux turns on and off, even though the electrons traverse only field-free regions. Spec Risk Notes: §12 is the epilogue — keep it tight. Don't over-explain QED; the goal is "here's where classical EM ends, here's the door to the next branch." The branch closes on §12.4 with one paragraph: "Maxwell wrote four equations. They survived relativity. They survived quantum mechanics. The classical theory is incomplete, but it is consistent — and it is the language every successor theory had to learn to speak." After §12: the branch is DONE. 66/66.
